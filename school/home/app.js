CREATE TABLE sport(
my id SERIEAL PRIMARY KEY,
sport name VARCHAR(50) UNIQUE NOT NULL,
numbers of players VARCHAR(50)UNIQUE NOT NULL,
type of sport  VARCHAR(50)UNIQUE NOT NULL,
created_on_DATE NOT NULL
);